### proxy multi protocol railway 

1.SOCKS5 Proxy (RFC 1928 & RFC 1929) with user password 

2.HTTPS Tunnel / HTTP CONNECT Proxy (with user password)

3. HTTP Forward / Reverse Proxy (HTTP SCAN) no user password
  
4. TLS SNI / Direct TCP Stream Proxy(no user password)

### cara install
fork tepo ini ke github mu lalau install di railway.com
buat domain railway arahkan k port 8080
buat domain tcp arahkan ke port 8080
redeploy agar ip muncul di ui uinya bisa d akses via proxy

bisa juga via url railway setup admin di dalam admin bisa setup 

user dan password proxy
